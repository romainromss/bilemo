app:
    resource: '@AppBundle/Actions/'
    type: annotation
    prefix: /api

login_check:
    path: /api/login_check

app.swagger_ui:
    path: /api/doc
    methods: GET
    defaults: { _controller: nelmio_api_doc.controller.swagger_ui }
